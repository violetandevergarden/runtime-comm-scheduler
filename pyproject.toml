[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "runtime-comm-scheduler"
version = "0.1.0"
description = "Prototype for runtime collective scheduling"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
